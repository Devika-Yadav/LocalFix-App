import 'package:flutter/material.dart';

// Dummy Survey Page
class SurveyPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Survey")),
      body: Center(child: Text("Survey Page")),
    );
  }
}
